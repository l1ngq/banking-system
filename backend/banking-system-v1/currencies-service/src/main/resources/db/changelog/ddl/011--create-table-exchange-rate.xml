<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                   https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="011--create-table-exchange-rate" author="student">
        <createTable tableName="exchange_rate" schemaName="${APP_DATABASE_SCHEMA}">
            <column name="id" type="bigint" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="base_currency" type="varchar(3)">
                <constraints nullable="false"/>
            </column>
            <column name="target_currency" type="varchar(3)">
                <constraints nullable="false"/>
            </column>
            <column name="rate" type="decimal(19,6)">
                <constraints nullable="false"/>
            </column>
            <column name="updated_at" type="timestamp with time zone">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <createIndex indexName="idx_exchange_rate_base_target"
                     tableName="exchange_rate"
                     schemaName="${APP_DATABASE_SCHEMA}"
                     unique="true">
            <column name="base_currency"/>
            <column name="target_currency"/>
        </createIndex>
    </changeSet>

</databaseChangeLog>
