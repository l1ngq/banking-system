---
- name: OS Baseline for all nodes
  hosts: k3s_cluster
  become: true
  roles:
    - setup-server

- name: Setup K3s Control Plane
  hosts: master
  become: true
  roles:
    - k3s_master

- name: Setup K3s Worker Nodes
  hosts: worker
  become: true
  roles:
    - k3s_worker
